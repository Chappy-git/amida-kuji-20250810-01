{
  "name": "amida-capacitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/copy-www.js",
    "ios:add": "npx cap add ios",
    "ios:copy": "npx cap copy ios",
    "ios:open": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  }
}